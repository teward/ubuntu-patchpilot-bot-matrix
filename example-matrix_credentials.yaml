# Store actual secrets in .matrix_credentials.yaml
HOMESERVER: "example.com"  # Base domain of your bot's matrix user id. For @bot:example.com, use example.com
USER_ID: "@FULL_BOT:USER.ID"
PASSWORD: "PASSWORD FOR YOUR BOT"
ROOM_ID: "INTERNAL_MATRIX_ROOM_ID"  # Bot will initially join here.